/**
 * Minimal slice panel for the verify:features contract fixtures (good case).
 * Read as source by core/__tests__/verifyFeaturesContract.test.ts; never
 * rendered by the suite.
 */
export function ContractFixturePanel() {
  return <div />;
}
